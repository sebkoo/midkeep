import SwiftUI

/// Stands in for what will live in `MidkeepUI`: the run list, the step
/// timeline, and the controls that stop a run in flight.
///
/// `MidkeepUI` may import SwiftUI and `MidkeepKit`, and nothing else. The
/// SwiftUI import is present from this commit because INV-3 permits it to
/// this module and unit 02 will use it.
struct Placeholder {}
